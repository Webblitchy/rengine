Inside the folder add the corresponding client certificate and key (client.crt and client.key) generated by the generateCertificate.sh script from the rengine-agent (they are outputted in the "cert_to_import" folder)
